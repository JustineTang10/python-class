#finding the greatest number
import random

list = []
maxnum = 0

for i in range(10):
  list.append(random.randint(1, 50))

for i in range(10):
  if list[i] > maxnum:
    maxnum = list[i]

print(list)
print("The greatest number is",maxnum)
